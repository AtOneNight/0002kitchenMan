mixin ProductCard(data)
	// start ProductCard
	+b.SECTION.ProductCard.section#ProductCard&attributes(attributes)
		.container
			+b.section-title.text-center
				h2 ProductCard
				
			.row
	// end ProductCard